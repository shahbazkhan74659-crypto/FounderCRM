# Current Tasks

## Active

None actively in progress. Phase 9 is done (see Completed); Phase 10 (Wiring Competitor Frontend to Backend) is next.

## Next

**Full roadmap locked (2026-09-13): Phase 0 through Phase 24.** Every phase's scope is documented in `PHASES.md` — not duplicated here, per this project's own responsibility-separation rule (`CLAUDE.md` rule 1). **None have been executed yet.** Execution starts only when the owner explicitly says so, phase by phase, in order.

Two things worth calling out because they cut across multiple phases:
- Most feature phases follow a backend → static frontend → wiring pattern, and most wiring phases explicitly defer real role-based permission enforcement to Phase 21, which resolves it everywhere at once.
- Phase 8's detailed endpoints/schema/logic, specifically, are left for the owner to specify when that phase starts (its `PHASES.md` entry only has a minimal scope).

Still **not yet** resolved:
- Any Phase 25+ roadmap — per the owner's explicit instruction (2026-09-13) and `CLAUDE.md` rule 3, `PHASES.md` may only be changed with the owner's explicit allowance; do not add to it unprompted.
- **Email delivery mechanism** for invite links and password-reset links (Phase 23) — not decided anywhere in this project yet; links are handled manually for now.

## Blocked

None.

## Completed

- [x] [Phase 9] Built the Competitor page frontend — list + detail views, local/sample data only — 2026-09-14. New `frontend/src/competitors/` directory: `CompetitorsPage`/`CompetitorList`/`CompetitorDetail`/`PricingChart`/`EditRequestsPanel` components, plus `types.ts`/`sample-data.ts`/`comparison.ts` (the last a client-side port of Phase 8's `buildComparison`, byte-for-byte matching caption phrasing). This is the first list↔detail local-state navigation in the codebase (no router exists) and the first time the top bar's Admin/Staff toggle became interactive — wired to a `viewRole` state threaded `App.tsx → AppShell → TopBar` (mirroring `onLogout`'s existing pattern), at the owner's explicit request, to actually switch `EditRequestsPanel` between the Staff (request form + "your requests") and Admin (approve/reject queue) views for the demo — local-only, not tied to real auth (Phase 21 unaffected). `App.tsx` now mounts `CompetitorsPage` in place of the old placeholder, finally using the `activeNavItem` prop unused since Phase 4. Ported `--success*`/`--warning-soft`/`--warning-text` tokens into `index.css` for delta/status-pill coloring. Verified end to end in-browser: list renders with correct deltas, detail view's chart/caption/history render correctly, Staff submitting a request appends it as pending, Admin approving/rejecting updates only that request's status (chart/history/positioning unchanged, matching Phase 8's deliberate non-auto-apply behavior — commented in code), back button works, and zero `/api/*` requests fire from any of it (confirmed via Network tab). `npm run build`/`npm run lint` clean. See `PHASES.md`.
- [x] [Phase 8] Built the full Competitor module backend — 2026-09-14. Owner defined the detailed scope at phase start (referencing the design prototype's Competitor-detail artboard, plus two follow-up clarifications on notifications and the "our pricing" table shape) — see `PHASES.md` for the full breakdown. 5 new tables (`competitors`, `competitor_pricing_history`, `company_pricing_plans`, `competitor_edit_requests`, `notifications` — the last one new scope for a notification mechanism), a shared `requireUser`/`requireAdmin` helper (`server/lib/api-auth.ts`), and 11 route files / 19 methods covering competitor CRUD, pricing-history CRUD, company-pricing-plans CRUD, the Staff-submit/Admin-approve-reject edit-request workflow, and notifications. Key deliberate behavior (owner's explicit instruction, diverging from the prototype): approving/rejecting an edit request only changes the request's own status and notifies the submitting staff member — it never auto-mutates the competitor/pricing data; Admin must separately use the direct-edit endpoints. Also fixed a real bug found along the way: `pg`'s default DATE parsing was shifting dates by a day via local-timezone round-tripping through JS `Date` — fixed globally with a type-parser override in `server/lib/db.ts`. Verified end to end via curl: full create → pricing history → comparison → edit request → approve/reject → notification flow; confirmed competitor data is byte-for-byte unchanged after approval; confirmed `409` on double-resolving a request, `404` on cross-competitor pricing-history access, and `401` on unauthenticated requests. `npm run build`/`npm run lint` clean. No frontend yet (Phase 9); Staff-submits-request path is only testable as the seeded Admin since Staff accounts can't be created until Phase 20 — a real testing limitation, not a defect. See `PHASES.md`.
- [x] [Phase 7 follow-up, scope exception] Added a logout flow, at the owner's explicit request — 2026-09-14. Backend: `POST /api/auth/logout` (`server/app/api/auth/logout/route.ts`) + a `deleteSession` helper (`server/lib/auth.ts`) delete the session row and clear the cookie. Frontend: a "Log out" button in `TopBar` (right of the Admin/Staff toggle, inside a new `.topbar-right` wrapper), wired through `AppShell` to a handler in `App.tsx` that calls the endpoint and always clears local auth state. Verified: logging out returns to the login page, and a refresh afterward stays logged out (`GET /api/auth/me` 401s — confirmed the DB session row was actually deleted, not just cleared client-side). `npm run build`/`npm run lint` clean on both `frontend/` and `server/`. Narrow, specific addition — not Phase 21's broader role-based-access work. See `PHASES.md`'s Phase 7 (`7d`).
- [x] [Phase 7 follow-up, scope exception] Fixed session persistence across a browser refresh, at the owner's explicit request, pulled forward from Phase 21 (which owns real role-based access — this doesn't overlap it) — 2026-09-14. New backend: `GET /api/auth/me` (`server/app/api/auth/me/route.ts`) + a `getSessionUser` helper (`server/lib/auth.ts`) validate the `foundercrm_session` cookie against the `sessions`/`users` tables and return the current user or `401`. New frontend: `App.tsx` calls this once on mount before deciding whether to show `LoginPage` or `AppShell`, so a valid session now survives a refresh. Confirmed via this Next.js version's own bundled docs (per `server/AGENTS.md`'s warning about breaking API changes) that `cookies()` from `next/headers` is async in this version and used it accordingly. Verified: no cookie → `401` (curl); valid session → `200` and the app loads straight into `AppShell` across repeated refreshes (checked via the Network tab). `npm run build`/`npm run lint` clean on both `frontend/` and `server/`. Does not add session-validation middleware for other routes or any role enforcement — that remains Phase 21's job, documented as an explicit boundary in `PHASES.md`'s `21a`. See `PHASES.md`'s Phase 7 (`7c`) and Phase 21 entries.
- [x] [Phase 7] Wired the standalone login page to the real login backend and finished the login flow end to end — 2026-09-14. `LoginPage` now performs a real `fetch('/api/auth/login', ...)` on submit (after local validation passes); backend errors ("Username and password are required" / "Invalid username or password") render verbatim in a new `.login-form-error` banner (new `--danger-soft` token, ported from the prototype), a thrown network error shows a generic fallback message, and the submit button disables with a "Signing in…" label while in flight. No manual token/cookie storage was added — the backend's `httpOnly` session cookie is stored automatically by the browser for the same-origin `fetch` (per Phase 5's deliberate cookie-only design, see `DECISIONS.md`). `App.tsx` now holds `isAuthenticated` state (plain `useState`, no router) and conditionally renders `LoginPage` (via a new `onLoginSuccess` callback prop) or `AppShell` wrapping the pre-Phase-6 placeholder paragraph. Verified end to end with a disposable test admin (`npm run seed:admin -- testadmin somepassword123`): invalid credentials show the error banner with no redirect; valid credentials transition into the full `AppShell` (top bar/sidebar/placeholder). Confirmed the expected limitation that a browser refresh after login returns to the login page (no session-check endpoint exists yet — deferred to Phase 21). `npm run build`/`npm run lint` clean. Session-validation middleware, logout, real dashboard content, and the (already-inert) "Forgot password?" button remain out of scope. See `PHASES.md`.
- [x] [Phase 6] Built the standalone login page UI: `LoginPage` (`frontend/src/auth/LoginPage.tsx` + `auth.css`) with controlled username/password inputs, inline required-field validation on submit, and no network call — 2026-09-14. `App.tsx` now mounts `LoginPage` directly in place of `AppShell` (owner-confirmed plain swap, no router); `AppShell`/`TopBar`/`Sidebar` are untouched, just unmounted for now. Added `--danger`/`--danger-text` tokens to `index.css` (ported from the prototype) for inline error styling; no login-specific prototype exists, so the visual design is self-designed from the existing token/`.card` system. Verified: `npm run build`/`npm run lint` clean, and manually in-browser (typing updates fields, empty submit shows both inline errors, fixing one field clears only that error, zero `/api/*` requests fire on submit). No real endpoint call, session handling, or redirect — all explicitly deferred to Phase 7 per Phase 6's locked scope. See `PHASES.md`.
- [x] [Phase 6 follow-ups] Owner-directed visual/UX refinements to the login page, all 2026-09-14, after the initial build: card shadow + accent "backlighting" glow; responsive mobile layout (fluid card capped 360px desktop / 240px mobile, with progressively tighter padding/gaps/sizing below 480px); best-effort autocomplete suppression (`autoComplete="off"` + readonly-until-focus on both inputs — Chrome ignores plain `autocomplete="off"` on login fields by design, so this is best-effort, not a guarantee); darkened field borders (`--text-faint`); and an inert "Forgot password?" button below the password field (scope exception — Phase 6 originally excluded forgot-password UI entirely; this is visual-only chrome with no handler, matching how `Sidebar`/`TopBar` render other not-yet-functional elements — the real flow stays Phase 23's job). See `PHASES.md`'s Phase 6 entry for the full breakdown.
- [x] [Phase 5] Built the login backend: `users`/`sessions` tables (`server/migrations/1789376319433_create-users-and-sessions.js`), one seeded Admin account (`server/scripts/seed-admin.mjs`), and `POST /api/auth/login` (`server/app/api/auth/login/route.ts` + `server/lib/auth.ts`) — 2026-09-14. Researched RajuApp's actual current auth implementation and mirrored it (`bcryptjs` cost 10, `crypto.randomBytes(32)` hex tokens, 30-day session TTL), with one owner-confirmed deviation: the session token is stored as a SHA-256 hash (`sessions.token_hash`), not RajuApp's plaintext; see `DECISIONS.md`. Login sets an `httpOnly` cookie (`foundercrm_session`) directly; wrong password and unknown username both return one generic `401`. Verified end to end: migration applied, admin seeded, successful login returns `200` + `Set-Cookie` + a matching `sessions` row, failures return the generic `401`/`400`, `npm run build`/`npm run lint` both clean. Also fixed a real bug found along the way: `server/lib/db.ts` was using `@next/env`'s `loadEnvConfig`, which nondeterministically stopped merging `DATABASE_URL` after Turbopack recompiles (see `DECISIONS.md`'s correction note on the DB-wiring decision) — switched to plain `dotenv`. No signup, logout, session-validation middleware, per-staff grants, or frontend login UI — all explicitly deferred per Phase 5's locked scope. See `PHASES.md`.
- [x] [Phase 4a] Built the shared frontend base layout (`AppShell`, composed of independent `TopBar` + `Sidebar` components under `frontend/src/layout/`) — 2026-09-14. Per the owner's explicit instruction that day ("The Base structure should be same as prototype"), the shell's visual design (fonts — Manrope/IBM Plex Sans — colors, spacing, measurements) mirrors `prototype/Main.dc.html` exactly rather than staying unstyled, overriding Phase 4's original visual-design exclusion; see `DECISIONS.md` for that decision and for how "islands-style component pattern" was concretely interpreted. Initially demonstrated shell reuse with two placeholder pages (`DashboardPage`/`FoundersPage`) plus a dev-only preview toggle; the owner then asked to remove both in favor of a bare structure, so `App.tsx` now mounts `AppShell` directly with a single generic placeholder line — see `PHASES.md`'s revised completion-criteria note. Sidebar nav items (Dashboard, Founders, Competitors, Analytics, Admin) are `<a href="#">` links, not wired to real navigation; the top bar's Admin/Staff toggle remains a non-interactive placeholder — no router, no real role logic. `npm run build` and `npm run lint` (`oxlint`) both verified clean after each round of changes. See `PHASES.md`.
- [x] [Out-of-order exception, before Phase 4] Connected the backend to the local Postgres database and wired dev-mode single-port serving, per the owner's explicit instruction (2026-09-14) — see `PHASES.md`'s "Out-of-order exception" note and `DECISIONS.md`. Specifically:
  - **Phase 1b (migration tooling)**: chose `node-pg-migrate` + `pg`; added `server/migrations/`, `server/lib/db.ts` (shared `pg.Pool`), and `npm run migrate:up`/`migrate:down`/`migrate:create` scripts (verified with `--dry-run`). No schema/tables created — still Phase 5's job.
  - **DB connectivity**: `GET /api/health` now also runs `SELECT 1` against Postgres and reports `db: "connected"`/`"error"` — verified locally (200 with `db: "connected"`).
  - **Single port (dev only)**: `server/next.config.ts` proxies non-API requests to the Vite dev server (`http://localhost:5173`) via a rewrite fallback; `frontend/vite.config.ts` pins the HMR websocket to connect directly to Vite; a root `package.json` (`concurrently`) runs both dev servers via one `npm run dev`. Verified: `http://localhost:3000` serves both the frontend placeholder and `/api/health`. Production single-port serving (`frontend/dist` → `server/public`) explicitly deferred, not done now.
- [x] [Phase 3a] Scaffolded the Vite + React + TypeScript frontend (`frontend/`, `npm create vite@latest -- --template react-ts`), replaced the template's demo content with a minimal placeholder page, and verified it end to end (`npm run dev`, `npm run build` → `frontend/dist`, `npm run lint`) — 2026-09-14. No wiring to the Phase 2 backend/API, no copying `dist` into `server/public`, and no real screens/components, per Phase 3's locked scope. See `PHASES.md`.
- [x] [Phase 2a] Scaffolded the Next.js API-only backend (`server/`, TypeScript, App Router, npm), stripped page-rendering artifacts, added `GET /api/health`, and verified it end to end (`npm run dev` + curl, `npm run build`, `npm run lint`) — 2026-09-13. No database wiring, real routes, frontend, or auth, per Phase 2's locked scope. See `PHASES.md`.
- [x] [Phase 1a] Confirmed local PostgreSQL 18 is installed and running (Windows service), created the local `foundercrm` database via `psql`, and confirmed connectivity and the connection-string/env-var convention for later phases (`DATABASE_URL`-style, matching `RajuApp`) — 2026-09-13. No tables and no migration tool were set up (1b deliberately not started, per the owner's instruction). See `PHASES.md`.
- [x] [Phase 0a] Gathered the core feature set and requirements directly from the owner: Admin/Staff roles and their permission boundaries, the founder-creation rule (any user), the competitor-data edit request/approval workflow, the Interview module's fields (notes, defined-question responses, date/time/count), analytics priorities (Budget Range, Feature Requests) with export marked as deferred, competitor pricing-history and future-pricing-comparison requirements, and the decision to defer both file storage and the database engine choice until after local development — 2026-09-13. See `PROJECT.md` and `DECISIONS.md`.
- [x] Set up this project's documentation system (`Project Docs/` — `CLAUDE.md`, `PROJECT.md`, `PHASES.md`, `TASKS.md`, `ARCHITECTURE.md`, `DECISIONS.md`), mirroring `C:\RajuApp`'s structure and conventions, populated with the requirements gathered above rather than left as an empty template — 2026-09-13. See `DECISIONS.md`'s "Documentation system" entry.
- [x] Stack-lock conversation with the owner: production stack (Vite + React + TypeScript frontend, Next.js API-only + Node.js backend, single repo/single deploy, Vitest), database engine (PostgreSQL, local), and hosting (Render + UptimeRobot) — 2026-09-13. Auth approach explicitly left open. See `DECISIONS.md`.
